# รายการสิ่งที่ควรปรับปรุงและพัฒนาเพิ่มเติมในระบบ (System Improvements & Backlog)

เอกสารรวบรวมฟีเจอร์ ปัญหา และแนวทางการปรับปรุงระบบ สำหรับวางแผนการพัฒนาในรอบถัดไป

---

## 1. การรองรับหลายภาษา (Localization / i18n)

* **ความต้องการ:** เพิ่มปุ่มสลับภาษาเพื่อรองรับกลุ่มผู้ใช้งานที่หลากหลาย
* **ขอบเขตงาน:**
  * ติดตั้ง UI สำหรับเลือก/สลับภาษา (เช่น บริเวณ Header หรือ Navigation bar)
  * ภาษาที่ต้องรองรับเพิ่มเติม:
    * ภาษาอังกฤษ (English)
    * ภาษาเมียนมา (Myanmar)
  * จัดทำโครงสร้าง Translation Files / Localization Store สำหรับ Frontend

---

## 2. ระบบ Dashboard แบบ Realtime

* **ปัญหาปัจจุบัน:** หน้า Dashboard ยังไม่อัปเดตข้อมูลแบบทันที
* **สาเหตุ:** ขาดการเชื่อมต่อ Supabase Realtime Subscription ภายใน Dashboard Component
* **ขอบเขตงาน:**
  * ทำการ Integrate Supabase Realtime (`supabase.channel().on(...)`) เข้ากับ Component
  * จัดการ State Management ให้ตอบสนองต่อ Event (`INSERT`, `UPDATE`, `DELETE`)
  * ปรับปรุง Fallback / Reconnection เพื่อรักษาความเสถียรของการแสดงผล

---

## 3. การป้องกันความผิดพลาดตอนสมัครสมาชิก (Registration UX & Confirmation Flow)

* **ปัญหาปัจจุบัน:** ผู้ใช้งานจำชื่อที่ใช้ Login ไม่ได้ หรือพิมพ์ข้อมูลผิดขณะ Register ส่งผลให้ไม่สามารถ Login ได้ในภายหลัง
* **แนวทางแก้ไข:**
  * เพิ่มขั้นตอน Confirmation Modal หรือ Summary Screen หลังกดปุ่ม Submit/Register
  * สรุปข้อมูลสำคัญให้ผู้ใช้ตรวจทานอีกครั้ง (เช่น Username, ชื่อ-นามสกุล, รหัสนักศึกษา, อีเมล)
  * Flow การทำงาน:
    1. ผู้ใช้กรอกแบบฟอร์ม Register
    2. กดปุ่มลงทะเบียน -> แสดงป๊อปอัปให้ตรวจสอบความถูกต้อง
    3. กดยืนยัน -> ส่งข้อมูลบันทึกลงระบบ แล้วจึง Redirect ไปหน้า Login ตามปกติ
    4. กดยกเลิก/แก้ไข -> กลับไปหน้าฟอร์มเดิมโดยคงค่าที่กรอกไว้

---

## 4. ปรับปรุงความชัดเจนของเงื่อนไขการรับรางวัล (Gamification Rules & Clarity)

* **ปัญหาปัจจุบัน:** ผู้เล่นบางส่วนยังไม่เข้าใจกติกาและเงื่อนไขการได้รับของรางวัล
* **ข้อจำกัด:** เงื่อนไขและของรางวัลอาจปรับเปลี่ยนทุกปีตาม Requirement ขององค์การนักศึกษา
* **แนวทางแก้ไข:**
  * เพิ่ม UI แสดงคำอธิบายกติกา (เช่น Modal กติกาตอนเข้าเล่น, Tooltip ช่วยเหลือ, หรือแถบ Progress Bar แจ้งสถานะภารกิจ)
  * ทำระบบจัดการข้อความกติกาและเงื่อนไขให้อยู่ในรูปแบบ Configurable Data (เช่น จัดการผ่าน Database/CMS หลังบ้าน) เพื่อให้แก้ไขได้ในแต่ละปีโดยไม่ต้อง Deploy โค้ดใหม่

---

## 5. ระบบประมวลผลและคัดกรองรายชื่ออัตโนมัติ (Post-Event Data Cleansing & Automation)

* **ความต้องการ:** พัฒนาระบบ Automation สำหรับจัดการและสรุปรายชื่อผู้เข้าร่วมหลังจบกิจกรรม
* **การคัดกรองและจัดหมวดหมู่ข้อมูล:**
  * **กลุ่มที่ 1: บัญชีที่ถูกต้อง (Valid Accounts):** ข้อมูลตรงกับฐานข้อมูลนักศึกษา และผ่านเกณฑ์เงื่อนไขครบถ้วน
  * **กลุ่มที่ 2: บัญชีที่น่าสงสัยว่าซ้ำซ้อน (Duplicate / Similar Accounts):** บัญชีที่มีชื่อ-นามสกุลใกล้เคียงกัน แต่พบหลายบัญชี (อาจใช้ String Similarity หรือ Match เลขประจำตัว)
  * **กลุ่มที่ 3: บัญชีที่ไม่ตรงกับทะเบียนนักศึกษา (Invalid / Unmatched Accounts):** บัญชีที่สะกดชื่อไม่ตรง หรือไม่มีในฐานข้อมูล เพื่อส่งต่อให้ทีมงานตรวจสอบแบบ Manual
* **ผลลัพธ์ที่ต้องการ:**
  * ระบบจัดกลุ่มสถานะอัตโนมัติ
  * ฟังก์ชัน Export รายชื่อพร้อมสถานะการคัดกรองเป็นไฟล์ Excel/CSV สำหรับองค์การนักศึกษา